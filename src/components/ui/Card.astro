---
interface Props {
  class?: string;
}

const { class: className = "" } = Astro.props;
---

<div
  class={`rounded-2xl bg-white p-8 shadow-lg transition duration-300 hover:-translate-y-2 hover:shadow-xl ${className}`}
>
  <slot />
</div>
