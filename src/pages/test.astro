---
import { getHero } from "@/services/home/hero.service";
import { getAbout } from "@/services/home/about.service";
import { getTimeline } from "@/services/home/timeline.service";
import {
  getProducts,
  getProductsByCategory,
  getFeaturedProduct,
  getProduct,
} from "@/services/products/products.service";
import { productRepository } from "@/scripts/products/product-repository";
// import { getFeaturedProduct, productMap } from "@/data/products/index";
import FeaturedProduct from "@/components/products/FeaturedProduct.astro";
import ProductsHeader from "@/components/products/ProductsHeader.astro";
import ProductFilters from "@/components/products/ProductFilters.astro";
import ProductGrid from "@/components/products/ProductGrid.astro";
import ProductCard from "@/components/products/ProductCard.astro";
import OffersSection from "@/sections/OffersSection.astro";
import Management from "@/sections/ManagementCouncilSection.astro";
import EventsSection from "@/sections/EventsSection.astro";
import HeroSlide from "@components/hero/HeroSlide.astro";
import ProductsSection from "@/sections/ProductsSection.astro";

// const hero = await getHero();
// const about = await getAbout();
// const timeline = await productRepository.getCategory("coffee-beans");
// const products = await productRepository.getProduct(
//   timeline.map((product) => product.id)[0],
// );
// productRepository.clearAll();
// const p =timeline.map((product) => product.id )[0]
// const featured = getFeaturedProduct("coffee-beans");
// const prs = [...productMap["coffee-beans"]];

// const productss = await getProducts();
// const coffeeProducts = productss.filter((p) => p.category === "coffee-beans");

// const featured = coffeeProducts.filter((p) => p.featured)[0];

// const grid = coffeeProducts.filter((p) => !p.featured);
---

<!-- <OffersSection /> -->
<pre>
    <!-- {...hero.loader} -->
       <!-- {hero.map((slide, index) => <HeroSlide slide={slide} index={index} />)} -->
<!-- {console.log(products.filter(product => product.data.category == 'coffee-beans').map(product=>product.data).find((p) => p.featured).specifications.filter(product => product.discriminant == 'origin')[0].value)} -->
<!-- {console.log(products.filter(product => product.data.category == 'coffee-beans'))} -->
 <!-- {featured.name}
 <img data-featured-image src={`src/content/products/${featured.name}/${featured.thumbnail}`} alt={featured.name} class="h-full w-full object-cover transition duration-700 hover:scale-105 object-center" /> -->
<!-- {featured.name} -->
 <!-- {timeline.map((product) => product.id )[0]} -->
</pre>
<!-- <FeaturedProduct />
<ProductsHeader />
<ProductFilters /> -->
<!-- {prs.map((product) => <ProductCard product={product} />)} -->
<!-- <ProductGrid products={prs} /> -->
<!-- <FeaturedProduct featured={featured} /> -->
 <!-- <Management /> -->
<!-- <ProductGrid products={grid} /> -->
